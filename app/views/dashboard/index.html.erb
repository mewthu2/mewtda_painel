<% title 'Dashboard Analytics' %>

<style>
  .db-page { padding: 24px 28px; min-height: 100vh; }

  .db-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 16px;
    margin-bottom: 24px;
  }
  .db-header__left { display: flex; flex-direction: column; gap: 4px; }
  .db-header__title {
    font-size: 22px;
    font-weight: 800;
    color: #0f172a;
    letter-spacing: -0.5px;
  }
  .db-header__subtitle {
    font-size: 13px;
    color: #64748b;
  }
  .db-header__right {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
  }

  .db-client-select {
    display: flex;
    align-items: center;
    gap: 8px;
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    padding: 6px 12px;
  }
  .db-client-select__label {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .5px;
    color: #94a3b8;
  }
  .db-client-select select {
    border: none;
    background: transparent;
    font-size: 13px;
    font-weight: 600;
    color: #0f172a;
    cursor: pointer;
    padding-right: 20px;
    appearance: none;
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
    background-position: right 0 center;
    background-repeat: no-repeat;
    background-size: 16px;
  }
  .db-client-select select:focus { outline: none; }

  .db-filters { display: flex; gap: 6px; }
  .db-filter-btn {
    padding: 7px 16px;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
    background: #fff;
    color: #475569;
    font-size: 13px;
    font-weight: 500;
    text-decoration: none;
    transition: all .15s;
  }
  .db-filter-btn:hover { background: #f1f5f9; border-color: #cbd5e1; color: #1e293b; }
  .db-filter-btn--active { background: #0f172a; color: #fff; border-color: #0f172a; }

  .db-empty {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 80px 40px;
    text-align: center;
  }
  .db-empty__icon {
    width: 80px;
    height: 80px;
    border-radius: 20px;
    background: #f1f5f9;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
  }
  .db-empty__icon svg { color: #94a3b8; }
  .db-empty__title {
    font-size: 18px;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 8px;
  }
  .db-empty__text {
    font-size: 14px;
    color: #64748b;
    max-width: 400px;
    line-height: 1.6;
  }

  .db-card {
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 20px;
  }
  .db-card__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }
  .db-card__title {
    font-size: 13px;
    font-weight: 600;
    color: #334155;
  }

  .db-kpi-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 16px;
    margin-bottom: 24px;
  }
  @media (max-width: 1100px) { .db-kpi-grid { grid-template-columns: repeat(3, 1fr); } }
  @media (max-width: 700px) { .db-kpi-grid { grid-template-columns: repeat(2, 1fr); } }

  .db-kpi {
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 18px 20px;
    position: relative;
    overflow: hidden;
  }
  .db-kpi::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
  }
  .db-kpi--blue::before { background: #3b82f6; }
  .db-kpi--green::before { background: #22c55e; }
  .db-kpi--amber::before { background: #f59e0b; }
  .db-kpi--rose::before { background: #f43f5e; }
  .db-kpi--purple::before { background: #8b5cf6; }

  .db-kpi__label {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .5px;
    color: #94a3b8;
    margin-bottom: 8px;
  }
  .db-kpi__value {
    font-size: 28px;
    font-weight: 800;
    color: #0f172a;
    line-height: 1;
    margin-bottom: 6px;
  }
  .db-kpi__change {
    font-size: 11px;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 3px;
    padding: 2px 6px;
    border-radius: 4px;
  }
  .db-kpi__change--up { background: #dcfce7; color: #15803d; }
  .db-kpi__change--down { background: #fee2e2; color: #dc2626; }
  .db-kpi__change--neutral { background: #f1f5f9; color: #64748b; }

  .db-funnel-section {
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    border-radius: 16px;
    padding: 28px;
    margin-bottom: 24px;
  }
  .db-funnel-section__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 24px;
  }
  .db-funnel-section__title {
    font-size: 14px;
    font-weight: 700;
    color: #fff;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .db-funnel-section__title svg { opacity: .6; }
  .db-funnel-section__client {
    font-size: 12px;
    color: #94a3b8;
    background: rgba(255,255,255,.08);
    padding: 6px 12px;
    border-radius: 6px;
  }
  .db-funnel-section__client strong { color: #fff; }

  .db-funnel {
    display: flex;
    align-items: stretch;
    gap: 0;
  }
  .db-funnel__step {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    padding: 0 12px;
  }
  .db-funnel__step + .db-funnel__step::before {
    content: '';
    position: absolute;
    left: 0;
    top: 30px;
    width: 1px;
    height: calc(100% - 60px);
    background: rgba(255,255,255,.1);
  }

  .db-funnel__icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 14px;
  }
  .db-funnel__count {
    font-size: 32px;
    font-weight: 800;
    color: #fff;
    line-height: 1;
    margin-bottom: 4px;
  }
  .db-funnel__name {
    font-size: 11px;
    color: #94a3b8;
    font-weight: 500;
    text-align: center;
    margin-bottom: 12px;
  }
  .db-funnel__bar {
    width: 100%;
    max-width: 80px;
    height: 6px;
    background: rgba(255,255,255,.1);
    border-radius: 3px;
    overflow: hidden;
    margin-bottom: 8px;
  }
  .db-funnel__bar-fill {
    height: 100%;
    border-radius: 3px;
    transition: width .5s ease;
  }
  .db-funnel__rate {
    font-size: 13px;
    font-weight: 700;
    color: #fff;
    margin-bottom: 4px;
  }
  .db-funnel__dropoff {
    font-size: 10px;
    color: #f87171;
    font-weight: 500;
  }
  .db-funnel__time {
    font-size: 10px;
    color: #60a5fa;
    margin-top: 6px;
    display: flex;
    align-items: center;
    gap: 3px;
  }

  .db-grid-2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-bottom: 24px;
  }
  .db-grid-3 {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-bottom: 24px;
  }
  @media (max-width: 1000px) {
    .db-grid-2, .db-grid-3 { grid-template-columns: 1fr; }
  }

  .db-chart-wrap { min-height: 240px; }

  .db-top-list { list-style: none; padding: 0; margin: 0; }
  .db-top-list li {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 0;
    border-bottom: 1px solid #f1f5f9;
    font-size: 13px;
  }
  .db-top-list li:last-child { border-bottom: none; }
  .db-top-list__rank {
    width: 22px;
    height: 22px;
    border-radius: 6px;
    background: #f1f5f9;
    color: #64748b;
    font-size: 11px;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 10px;
    flex-shrink: 0;
  }
  .db-top-list__title {
    flex: 1;
    color: #334155;
    font-weight: 500;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 180px;
  }
  .db-top-list__count {
    font-weight: 700;
    color: #0f172a;
  }

  .db-top-list--products li {
    padding: 12px 0;
  }
  .db-top-list__img {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    object-fit: cover;
    margin-right: 12px;
    flex-shrink: 0;
    background: #f1f5f9;
  }
  .db-top-list__img-placeholder {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    background: #f1f5f9;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 12px;
    flex-shrink: 0;
    color: #94a3b8;
  }
  .db-top-list__info {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .db-top-list--products .db-top-list__title {
    max-width: none;
    font-size: 13px;
    font-weight: 600;
    color: #0f172a;
  }
  .db-top-list__price {
    font-size: 11px;
    color: #64748b;
  }
  .db-top-list--products .db-top-list__count {
    font-size: 14px;
    font-weight: 700;
    color: #0f172a;
    background: #f1f5f9;
    padding: 4px 10px;
    border-radius: 6px;
    margin-left: 8px;
  }

  .db-heatmap {
    display: grid;
    grid-template-columns: repeat(24, 1fr);
    gap: 3px;
  }
  .db-heatmap__cell {
    aspect-ratio: 1;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 9px;
    font-weight: 600;
    color: #64748b;
    position: relative;
    cursor: default;
  }
  .db-heatmap__cell:hover::after {
    content: attr(data-tooltip);
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    background: #0f172a;
    color: #fff;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 10px;
    white-space: nowrap;
    z-index: 10;
  }
  .db-heatmap__labels {
    display: grid;
    grid-template-columns: repeat(24, 1fr);
    gap: 3px;
    margin-top: 4px;
  }
  .db-heatmap__label {
    text-align: center;
    font-size: 9px;
    color: #94a3b8;
  }

  .db-table-card {
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 24px;
  }
  .db-table-card__header {
    padding: 16px 20px;
    border-bottom: 1px solid #f1f5f9;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .db-table-card__title { font-size: 14px; font-weight: 700; color: #0f172a; }
  .db-table-card__count { font-size: 12px; color: #94a3b8; }
  .db-table {
    width: 100%;
    border-collapse: collapse;
  }
  .db-table thead th {
    padding: 12px 16px;
    text-align: left;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .6px;
    color: #94a3b8;
    background: #fff;
    border-bottom: 1px solid #e2e8f0;
  }
  .db-table thead th.center { text-align: center; }
  .db-table tbody tr {
    border-bottom: 1px solid #f1f5f9;
    transition: background .1s;
    cursor: pointer;
  }
  .db-table tbody tr:last-child { border-bottom: none; }
  .db-table tbody tr:hover { background: #f1f5f9; }
  .db-table tbody tr.db-table__row--abandoned { background: #fef2f2; }
  .db-table tbody tr.db-table__row--abandoned:hover { background: #fee2e2; }
  .db-table td {
    padding: 14px 16px;
    font-size: 13px;
    color: #475569;
    vertical-align: middle;
  }
  .db-table td.center { text-align: center; }
  .db-table__empty {
    text-align: center;
    padding: 48px;
    color: #94a3b8;
    font-size: 14px;
  }

  .db-badge {
    display: inline-block;
    padding: 3px 8px;
    border-radius: 6px;
    font-size: 11px;
    font-weight: 600;
  }
  .db-badge--mono { font-family: monospace; background: #f1f5f9; color: #475569; }
  .db-badge--page    { background: #dbeafe; color: #1d4ed8; }
  .db-badge--product { background: #dcfce7; color: #15803d; }
  .db-badge--cart    { background: #fef3c7; color: #b45309; }
  .db-badge--checkout{ background: #fce7f3; color: #be185d; }
  .db-badge--count   { background: #0f172a; color: #fff; }
  .db-badge--abandoned { background: #fee2e2; color: #dc2626; }
  .db-badge--device  { background: #f1f5f9; color: #64748b; font-size: 10px; }

  .db-modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(15,23,42,.6);
    backdrop-filter: blur(4px);
    z-index: 1000;
    overflow-y: auto;
  }
  .db-modal {
    background: #fff;
    max-width: 820px;
    margin: 40px auto;
    border-radius: 16px;
    box-shadow: 0 25px 80px rgba(0,0,0,.25);
    overflow: hidden;
  }
  .db-modal__head {
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    padding: 24px;
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
  }
  .db-modal__head-title { font-size: 16px; font-weight: 800; color: #fff; margin: 0 0 4px; }
  .db-modal__head-sid { font-size: 11px; color: #64748b; font-family: monospace; word-break: break-all; }
  .db-modal__close {
    background: rgba(255,255,255,.1);
    border: none;
    color: #fff;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    font-size: 18px;
    cursor: pointer;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background .15s;
  }
  .db-modal__close:hover { background: rgba(255,255,255,.2); }
  .db-modal__meta {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    padding: 16px 24px;
    border-bottom: 1px solid #f1f5f9;
  }
  .db-modal__chip {
    font-size: 12px;
    background: #fff;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    padding: 8px 12px;
    color: #475569;
  }
  .db-modal__chip strong { color: #0f172a; }
  .db-modal__body { padding: 24px; }
  .db-modal__section-title {
    font-size: 11px;
    font-weight: 700;
    color: #94a3b8;
    text-transform: uppercase;
    letter-spacing: .6px;
    margin: 0 0 16px;
  }
  .db-modal__loading { text-align: center; padding: 56px; color: #94a3b8; font-size: 14px; }

  .db-event {
    border: 1px solid #e2e8f0;
    border-radius: 10px;
    padding: 16px;
    margin-bottom: 12px;
    background: #fff;
    transition: box-shadow .15s;
  }
  .db-event:hover { box-shadow: 0 2px 8px rgba(0,0,0,.04); }
  .db-event__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 12px;
  }
  .db-event__time { font-size: 11px; color: #94a3b8; }
  .db-event__row { font-size: 12px; color: #475569; margin-bottom: 6px; line-height: 1.5; }
  .db-event__row a { color: #3b82f6; }
  .db-event__row--muted { color: #94a3b8; word-break: break-all; font-size: 11px; }
  .db-event summary { font-size: 12px; color: #64748b; cursor: pointer; user-select: none; margin-top: 8px; }
  .db-event pre {
    background: #f1f5f9;
    border: 1px solid #e2e8f0;
    border-radius: 6px;
    padding: 12px;
    font-size: 11px;
    overflow: auto;
    margin: 8px 0 0;
    line-height: 1.5;
  }
</style>


<div class="db-page">

  <div class="db-header">
    <div class="db-header__left">
      <h1 class="db-header__title">Funil de Conversao</h1>
      <p class="db-header__subtitle">
        <% if @client %>
          Dados de <strong><%= @client.name %></strong>
        <% else %>
          Analise o comportamento dos seus clientes
        <% end %>
      </p>
    </div>

    <div class="db-header__right">
      <% if @is_admin && @clients.present? %>
        <div class="db-client-select">
          <span class="db-client-select__label">Cliente</span>
          <select id="client-selector" onchange="window.location.href = this.value">
            <% @clients.each do |client| %>
              <option
                value="<%= painel_path(client_id: client.id, period: @period) %>"
                <%= 'selected' if client.id == @selected_client_id %>
              >
                <%= client.name %>
              </option>
            <% end %>
          </select>
        </div>
      <% end %>

      <div class="db-filters">
        <%= link_to "Hoje",    painel_path(client_id: @selected_client_id, period: "1"),  class: "db-filter-btn #{'db-filter-btn--active' if @period == '1'}" %>
        <%= link_to "7 dias",  painel_path(client_id: @selected_client_id, period: "7"),  class: "db-filter-btn #{'db-filter-btn--active' if @period == '7'}" %>
        <%= link_to "15 dias", painel_path(client_id: @selected_client_id, period: "15"), class: "db-filter-btn #{'db-filter-btn--active' if @period == '15'}" %>
        <%= link_to "30 dias", painel_path(client_id: @selected_client_id, period: "30"), class: "db-filter-btn #{'db-filter-btn--active' if @period == '30'}" %>
      </div>
    </div>
  </div>


  <% if @empty_state %>
    <div class="db-card">
      <div class="db-empty">
        <div class="db-empty__icon">
          <svg width="40" height="40" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
            <path d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 013 19.875v-6.75z"/>
            <path d="M9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V8.625z"/>
            <path d="M16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V4.125z"/>
          </svg>
        </div>
        <h2 class="db-empty__title">Nenhum dado disponivel</h2>
        <p class="db-empty__text"><%= @empty_message %></p>
      </div>
    </div>

  <% else %>

    <%
      def calc_change(current, previous)
        return { value: 0, direction: 'neutral' } if previous.zero?
        change = ((current - previous).to_f / previous * 100).round(1)
        { value: change.abs, direction: change > 0 ? 'up' : (change < 0 ? 'down' : 'neutral') }
      end

      sessions_change = calc_change(@unique_sessions, @prev_unique_sessions)
      checkout_change = calc_change(@counts[:checkout_completed], @prev_counts[:checkout_completed])
    %>

    <div class="db-kpi-grid">
      <div class="db-kpi db-kpi--blue">
        <div class="db-kpi__label">Sessoes Unicas</div>
        <div class="db-kpi__value"><%= number_with_delimiter(@unique_sessions) %></div>
        <span class="db-kpi__change db-kpi__change--<%= sessions_change[:direction] %>">
          <% if sessions_change[:direction] == 'up' %>+<% elsif sessions_change[:direction] == 'down' %>-<% end %><%= sessions_change[:value] %>%
        </span>
      </div>

      <div class="db-kpi db-kpi--green">
        <div class="db-kpi__label">Checkouts</div>
        <div class="db-kpi__value"><%= number_with_delimiter(@counts[:checkout_completed]) %></div>
        <span class="db-kpi__change db-kpi__change--<%= checkout_change[:direction] %>">
          <% if checkout_change[:direction] == 'up' %>+<% elsif checkout_change[:direction] == 'down' %>-<% end %><%= checkout_change[:value] %>%
        </span>
      </div>

      <div class="db-kpi db-kpi--amber">
        <div class="db-kpi__label">Taxa Conversao</div>
        <div class="db-kpi__value"><%= @conversion[:checkout_completed] %>%</div>
        <span class="db-kpi__change db-kpi__change--neutral">do total</span>
      </div>

      <div class="db-kpi db-kpi--rose">
        <div class="db-kpi__label">Nao Convertidos</div>
        <div class="db-kpi__value"><%= @abandoned_carts %></div>
        <span class="db-kpi__change db-kpi__change--down"><%= @abandoned_rate %>% taxa</span>
      </div>

      <div class="db-kpi db-kpi--purple">
        <div class="db-kpi__label">Total Eventos</div>
        <div class="db-kpi__value"><%= number_with_delimiter(@counts.values.sum) %></div>
        <span class="db-kpi__change db-kpi__change--neutral">no periodo</span>
      </div>
    </div>


    <div class="db-funnel-section">
      <div class="db-funnel-section__header">
        <div class="db-funnel-section__title">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M22 3H2l8 9.46V19l4 2v-8.54L22 3z"/></svg>
          Funil de Conversao
        </div>
        <% if @client %>
          <div class="db-funnel-section__client">
            Cliente: <strong><%= @client.name %></strong>
          </div>
        <% end %>
      </div>

      <div class="db-funnel">
        <div class="db-funnel__step">
          <div class="db-funnel__icon" style="background:rgba(59,130,246,.15)">
            <svg width="20" height="20" fill="none" stroke="#3b82f6" stroke-width="2" viewBox="0 0 24 24"><path d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0h6"/></svg>
          </div>
          <div class="db-funnel__count"><%= number_with_delimiter(@counts[:page_viewed]) %></div>
          <div class="db-funnel__name">Paginas Vistas</div>
          <div class="db-funnel__bar">
            <div class="db-funnel__bar-fill" style="width:100%; background:#3b82f6"></div>
          </div>
          <div class="db-funnel__rate">100%</div>
        </div>

        <div class="db-funnel__step">
          <div class="db-funnel__icon" style="background:rgba(34,197,94,.15)">
            <svg width="20" height="20" fill="none" stroke="#22c55e" stroke-width="2" viewBox="0 0 24 24"><path d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>
          </div>
          <div class="db-funnel__count"><%= number_with_delimiter(@counts[:product_viewed]) %></div>
          <div class="db-funnel__name">Produtos Vistos</div>
          <div class="db-funnel__bar">
            <div class="db-funnel__bar-fill" style="width:<%= @conversion[:product_viewed] %>%; background:#22c55e"></div>
          </div>
          <div class="db-funnel__rate"><%= @conversion[:product_viewed] %>%</div>
          <div class="db-funnel__dropoff"><%= @dropoff[:page_to_product] %>% saiu</div>
          <% if @avg_times[:page_to_product] > 0 %>
            <div class="db-funnel__time">
              <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
              <%= @avg_times[:page_to_product] %> min
            </div>
          <% end %>
        </div>

        <div class="db-funnel__step">
          <div class="db-funnel__icon" style="background:rgba(245,158,11,.15)">
            <svg width="20" height="20" fill="none" stroke="#f59e0b" stroke-width="2" viewBox="0 0 24 24"><path d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13l-1.35 2.7A1 1 0 007 17h11M7 13l-.4-2M17 17a2 2 0 100 4 2 2 0 000-4zm-8 0a2 2 0 100 4 2 2 0 000-4z"/></svg>
          </div>
          <div class="db-funnel__count"><%= number_with_delimiter(@counts[:added_to_cart]) %></div>
          <div class="db-funnel__name">Add ao Carrinho</div>
          <div class="db-funnel__bar">
            <div class="db-funnel__bar-fill" style="width:<%= @conversion[:added_to_cart] %>%; background:#f59e0b"></div>
          </div>
          <div class="db-funnel__rate"><%= @conversion[:added_to_cart] %>%</div>
          <div class="db-funnel__dropoff"><%= @dropoff[:product_to_cart] %>% saiu</div>
          <% if @avg_times[:product_to_cart] > 0 %>
            <div class="db-funnel__time">
              <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
              <%= @avg_times[:product_to_cart] %> min
            </div>
          <% end %>
        </div>

        <div class="db-funnel__step">
          <div class="db-funnel__icon" style="background:rgba(236,72,153,.15)">
            <svg width="20" height="20" fill="none" stroke="#ec4899" stroke-width="2" viewBox="0 0 24 24"><path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          </div>
          <div class="db-funnel__count"><%= number_with_delimiter(@counts[:checkout_completed]) %></div>
          <div class="db-funnel__name">Checkout</div>
          <div class="db-funnel__bar">
            <div class="db-funnel__bar-fill" style="width:<%= @conversion[:checkout_completed] %>%; background:#ec4899"></div>
          </div>
          <div class="db-funnel__rate"><%= @conversion[:checkout_completed] %>%</div>
          <div class="db-funnel__dropoff"><%= @dropoff[:cart_to_checkout] %>% nao converteu</div>
          <% if @avg_times[:cart_to_checkout] > 0 %>
            <div class="db-funnel__time">
              <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
              <%= @avg_times[:cart_to_checkout] %> min
            </div>
          <% end %>
        </div>
      </div>
    </div>


    <div class="db-grid-2">
      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Eventos por Dia</span>
        </div>
        <div id="events_chart" class="db-chart-wrap"></div>
      </div>

      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Dispositivos</span>
        </div>
        <div id="devices_chart" class="db-chart-wrap"></div>
      </div>
    </div>


    <div class="db-grid-3">
      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Origem do Trafego</span>
        </div>
        <div id="referrers_chart" class="db-chart-wrap"></div>
      </div>

      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Top Produtos Vistos</span>
        </div>
        <% if @top_products_viewed.any? %>
          <ul class="db-top-list db-top-list--products">
            <% @top_products_viewed.each_with_index do |p, i| %>
              <li>
                <span class="db-top-list__rank"><%= i + 1 %></span>
                <% if p[:image].present? %>
                  <img
                    src="<%= p[:image].start_with?('//') ? "https:#{p[:image]}" : p[:image] %>"
                    alt="<%= p[:title] %>"
                    class="db-top-list__img"
                    loading="lazy"
                  />
                <% else %>
                  <span class="db-top-list__img-placeholder">
                    <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>
                  </span>
                <% end %>
                <div class="db-top-list__info">
                  <span class="db-top-list__title"><%= p[:title] %></span>
                  <% if p[:price].present? && p[:price] > 0 %>
                    <span class="db-top-list__price">
                      <%= number_to_currency(p[:price], unit: "R$ ", separator: ",", delimiter: ".") %>
                    </span>
                  <% end %>
                </div>
                <span class="db-top-list__count"><%= p[:count] %></span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p style="color:#94a3b8; font-size:13px; text-align:center; padding:32px 0">Sem dados</p>
        <% end %>
      </div>

      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Top Produtos no Carrinho</span>
        </div>
        <% if @top_products_carted.any? %>
          <ul class="db-top-list db-top-list--products">
            <% @top_products_carted.each_with_index do |p, i| %>
              <li>
                <span class="db-top-list__rank"><%= i + 1 %></span>
                <% if p[:image].present? %>
                  <img
                    src="<%= p[:image].start_with?('//') ? "https:#{p[:image]}" : p[:image] %>"
                    alt="<%= p[:title] %>"
                    class="db-top-list__img"
                    loading="lazy"
                  />
                <% else %>
                  <span class="db-top-list__img-placeholder">
                    <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13l-1.35 2.7A1 1 0 007 17h11M7 13l-.4-2M17 17a2 2 0 100 4 2 2 0 000-4zm-8 0a2 2 0 100 4 2 2 0 000-4z"/></svg>
                  </span>
                <% end %>
                <div class="db-top-list__info">
                  <span class="db-top-list__title"><%= p[:title] %></span>
                  <% if p[:price].present? && p[:price] > 0 %>
                    <span class="db-top-list__price">
                      <%= number_to_currency(p[:price], unit: "R$ ", separator: ",", delimiter: ".") %>
                    </span>
                  <% end %>
                </div>
                <span class="db-top-list__count"><%= p[:count] %></span>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p style="color:#94a3b8; font-size:13px; text-align:center; padding:32px 0">Sem dados</p>
        <% end %>
      </div>
    </div>


    <div class="db-card" style="margin-bottom:24px">
      <div class="db-card__header">
        <span class="db-card__title">Atividade por Hora do Dia</span>
      </div>
      <% max_hour = @hourly_events.values.max || 1 %>
      <div class="db-heatmap">
        <% @hourly_events.each do |hour, count| %>
          <%
            intensity = count.to_f / max_hour
            bg = if intensity == 0
              '#f1f5f9'
            elsif intensity < 0.25
              '#dbeafe'
            elsif intensity < 0.5
              '#93c5fd'
            elsif intensity < 0.75
              '#3b82f6'
            else
              '#1d4ed8'
            end
            text_color = intensity >= 0.5 ? '#fff' : '#64748b'
          %>
          <div
            class="db-heatmap__cell"
            style="background:<%= bg %>; color:<%= text_color %>"
            data-tooltip="<%= hour %>h: <%= count %> eventos"
          >
            <%= count > 0 ? count : '' %>
          </div>
        <% end %>
      </div>
      <div class="db-heatmap__labels">
        <% (0..23).each do |h| %>
          <span class="db-heatmap__label"><%= h.to_s.rjust(2, '0') %></span>
        <% end %>
      </div>
    </div>


    <div class="db-grid-2">
      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Atividade por Dia da Semana</span>
        </div>
        <div id="weekday_chart" class="db-chart-wrap"></div>
      </div>

      <div class="db-card">
        <div class="db-card__header">
          <span class="db-card__title">Funil Comparativo</span>
        </div>
        <div id="funnel_bar_chart" class="db-chart-wrap"></div>
      </div>
    </div>


    <div class="db-table-card">
      <div class="db-table-card__header">
        <span class="db-table-card__title">Sessoes Recentes</span>
        <span class="db-table-card__count"><%= @sessions.size %> sessoes | <%= @sessions.count { |s| !s[:abandoned] } %> convertidas</span>
      </div>

      <table class="db-table">
        <thead>
          <tr>
            <th>Sessao</th>
            <th>Dominio</th>
            <th>Jornada</th>
            <th class="center">Device</th>
            <th class="center">Duracao</th>
            <th class="center">Eventos</th>
            <th class="center">Status</th>
          </tr>
        </thead>

        <tbody>
          <% if @sessions.empty? %>
            <tr>
              <td colspan="7" class="db-table__empty">
                Nenhuma sessao encontrada no periodo selecionado.
              </td>
            </tr>
          <% else %>
            <% @sessions.each do |s| %>
              <tr
                class="db-session-row <%= 'db-table__row--abandoned' if s[:abandoned] %>"
                data-session-id="<%= s[:session_id] %>"
                data-fetch-url="<%= painel_session_path(session_id: s[:session_id], client_id: @selected_client_id) %>"
              >
                <td>
                  <span class="db-badge db-badge--mono"><%= s[:session_id].truncate(12) %></span>
                </td>
                <td style="font-weight:500; color:#0f172a"><%= s[:shop_domain] %></td>
                <td>
                  <div style="display:flex; gap:4px; flex-wrap:wrap">
                    <% s[:last_kinds].each do |kind| %>
                      <%
                        badge_class = case kind
                          when 'page_viewed'           then 'db-badge--page'
                          when 'product_viewed'        then 'db-badge--product'
                          when 'product_added_to_cart' then 'db-badge--cart'
                          when 'checkout_started'      then 'db-badge--cart'
                          when 'checkout_completed'    then 'db-badge--checkout'
                          else ''
                        end
                      %>
                      <span class="db-badge <%= badge_class %>"><%= kind.split('_').first(2).join(' ') %></span>
                    <% end %>
                  </div>
                </td>
                <td class="center">
                  <span class="db-badge db-badge--device"><%= s[:device] %></span>
                </td>
                <td class="center" style="color:#64748b; font-size:12px"><%= s[:duration_min] %> min</td>
                <td class="center">
                  <span class="db-badge db-badge--count"><%= s[:event_count] %></span>
                </td>
                <td class="center">
                  <% if s[:abandoned] %>
                    <span class="db-badge db-badge--abandoned">nao converteu</span>
                  <% else %>
                    <span class="db-badge" style="background:#dcfce7; color:#15803d">converteu</span>
                  <% end %>
                </td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
    </div>

  <% end %>

</div>


<div id="db-modal-overlay" class="db-modal-overlay" role="dialog" aria-modal="true">
  <div class="db-modal">
    <div class="db-modal__head">
      <div>
        <h2 class="db-modal__head-title">Detalhes da Sessao</h2>
        <p id="db-modal-sid" class="db-modal__head-sid"></p>
      </div>
      <button id="db-modal-close" class="db-modal__close">&times;</button>
    </div>
    <div id="db-modal-meta" class="db-modal__meta"></div>
    <div class="db-modal__body">
      <div id="db-modal-loading" class="db-modal__loading">Carregando...</div>
      <div id="db-modal-events" style="display:none">
        <p class="db-modal__section-title">Timeline de Eventos</p>
        <div id="db-modal-events-list"></div>
      </div>
    </div>
  </div>
</div>


<% unless @empty_state %>
<script>
document.addEventListener("DOMContentLoaded", function () {
  var COLORS = ['#3b82f6', '#22c55e', '#f59e0b', '#ec4899', '#8b5cf6'];

  new ApexCharts(document.querySelector("#events_chart"), {
    chart: { type: 'area', height: 220, toolbar: { show: false }, fontFamily: 'inherit' },
    series: [
      { name: 'Page',     data: <%= raw @daily_events.select{|k,_| k[1]=='page_viewed'}.map{|k,v| [k[0],v]}.to_json %> },
      { name: 'Produto',  data: <%= raw @daily_events.select{|k,_| k[1]=='product_viewed'}.map{|k,v| [k[0],v]}.to_json %> },
      { name: 'Carrinho', data: <%= raw @daily_events.select{|k,_| k[1]=='product_added_to_cart'}.map{|k,v| [k[0],v]}.to_json %> },
      { name: 'Checkout', data: <%= raw @daily_events.select{|k,_| k[1]=='checkout_completed'}.map{|k,v| [k[0],v]}.to_json %> }
    ],
    xaxis: { type: 'datetime', labels: { style: { fontSize: '11px' } } },
    colors: COLORS,
    fill: { type: 'gradient', gradient: { shadeIntensity: 1, opacityFrom: 0.3, opacityTo: 0.05 } },
    stroke: { width: 2, curve: 'smooth' },
    legend: { fontSize: '11px', position: 'top' },
    grid: { borderColor: '#f1f5f9' },
    tooltip: { theme: 'light', x: { format: 'dd/MM' } }
  }).render();

  new ApexCharts(document.querySelector("#devices_chart"), {
    chart: { type: 'donut', height: 220, fontFamily: 'inherit' },
    series: [<%= @devices[:desktop] %>, <%= @devices[:mobile] %>, <%= @devices[:tablet] %>],
    labels: ['Desktop', 'Mobile', 'Tablet'],
    colors: ['#3b82f6', '#22c55e', '#f59e0b'],
    legend: { position: 'bottom', fontSize: '12px' },
    plotOptions: { pie: { donut: { size: '60%', labels: { show: true, total: { show: true, label: 'Total', fontSize: '14px', fontWeight: 700 } } } } },
    dataLabels: { enabled: false }
  }).render();

  new ApexCharts(document.querySelector("#referrers_chart"), {
    chart: { type: 'bar', height: 220, toolbar: { show: false }, fontFamily: 'inherit' },
    series: [{ name: 'Visitas', data: <%= raw @referrers.values.to_json %> }],
    xaxis: { categories: <%= raw @referrers.keys.to_json %> },
    colors: ['#8b5cf6'],
    plotOptions: { bar: { horizontal: true, borderRadius: 4 } },
    grid: { borderColor: '#f1f5f9' },
    dataLabels: { enabled: true, style: { fontSize: '11px' } }
  }).render();

  new ApexCharts(document.querySelector("#weekday_chart"), {
    chart: { type: 'bar', height: 200, toolbar: { show: false }, fontFamily: 'inherit' },
    series: [{ name: 'Eventos', data: <%= raw @weekday_events.map{|d| d[:count]}.to_json %> }],
    xaxis: { categories: <%= raw @weekday_events.map{|d| d[:day]}.to_json %> },
    colors: ['#3b82f6'],
    plotOptions: { bar: { borderRadius: 4, columnWidth: '55%' } },
    grid: { borderColor: '#f1f5f9' },
    dataLabels: { enabled: false }
  }).render();

  new ApexCharts(document.querySelector("#funnel_bar_chart"), {
    chart: { type: 'bar', height: 200, toolbar: { show: false }, fontFamily: 'inherit' },
    series: [{ name: 'Eventos', data: [<%= @counts[:page_viewed] %>, <%= @counts[:product_viewed] %>, <%= @counts[:added_to_cart] %>, <%= @counts[:checkout_completed] %>] }],
    xaxis: { categories: ['Paginas', 'Produtos', 'Carrinho', 'Checkout'] },
    colors: COLORS,
    plotOptions: { bar: { distributed: true, borderRadius: 6, columnWidth: '50%' } },
    legend: { show: false },
    grid: { borderColor: '#f1f5f9' },
    dataLabels: { enabled: true, style: { fontSize: '12px', fontWeight: 700 } }
  }).render();
});
</script>

<script>
(function () {
  var KIND_BADGE = {
    page_viewed:           'db-badge--page',
    product_viewed:        'db-badge--product',
    product_added_to_cart: 'db-badge--cart',
    checkout_started:      'db-badge--cart',
    checkout_completed:    'db-badge--checkout'
  };

  function fmtDate(str) { return new Date(str).toLocaleString('pt-BR'); }

  function chip(label, value) {
    return '<div class="db-modal__chip"><span style="color:#94a3b8">' + label + '</span> <strong>' + value + '</strong></div>';
  }

  function renderEvent(e) {
    var ctx = e.context || {}, doc = ctx.document || {}, nav = ctx.navigator || {}, win = ctx.window || {}, loc = doc.location || {}, scr = win.screen || {};
    var url = loc.href, title = doc.title, referrer = doc.referrer, ua = nav.userAgent;
    var screen = (scr.width && scr.height) ? scr.width + 'x' + scr.height : null;
    var hasData = e.data && typeof e.data === 'object' && Object.keys(e.data).length > 0;
    var badgeCls = KIND_BADGE[e.kind] || '';

    var html = '<div class="db-event">';
    html += '<div class="db-event__head">';
    html += '<span class="db-badge ' + badgeCls + '">' + (e.kind || '').replace(/_/g,' ') + '</span>';
    html += '<span class="db-event__time">' + fmtDate(e.created_at) + '</span>';
    html += '</div>';
    if (title) html += '<div class="db-event__row"><strong>Titulo:</strong> ' + title + '</div>';
    if (url) html += '<div class="db-event__row" style="word-break:break-all"><strong>URL:</strong> <a href="' + url + '" target="_blank">' + url + '</a></div>';
    if (referrer) html += '<div class="db-event__row" style="word-break:break-all"><strong>Referrer:</strong> ' + referrer + '</div>';
    if (screen) html += '<div class="db-event__row"><strong>Tela:</strong> ' + screen + '</div>';
    if (ua) html += '<div class="db-event__row db-event__row--muted">' + ua + '</div>';
    if (hasData) {
      html += '<details><summary>Ver dados</summary><pre>' + JSON.stringify(e.data, null, 2) + '</pre></details>';
    }
    html += '</div>';
    return html;
  }

  document.querySelectorAll('.db-session-row').forEach(function (row) {
    row.addEventListener('click', function () {
      var sid = this.dataset.sessionId, url = this.dataset.fetchUrl;
      document.getElementById('db-modal-sid').textContent = sid;
      document.getElementById('db-modal-meta').innerHTML = '';
      document.getElementById('db-modal-events-list').innerHTML = '';
      document.getElementById('db-modal-loading').style.display = 'block';
      document.getElementById('db-modal-loading').textContent = 'Carregando...';
      document.getElementById('db-modal-events').style.display = 'none';
      document.getElementById('db-modal-overlay').style.display = 'block';
      document.body.style.overflow = 'hidden';

      fetch(url, { method: 'GET', credentials: 'same-origin', headers: { 'Accept': 'application/json', 'X-CSRF-Token': (document.querySelector('meta[name="csrf-token"]') || {}).content || '' } })
        .then(function (r) { if (!r.ok) throw new Error('HTTP ' + r.status); return r.json(); })
        .then(function (data) {
          var meta = '';
          meta += chip('Dominio', data.shop_domain || '-');
          meta += chip('Inicio', fmtDate(data.started_at));
          meta += chip('Fim', fmtDate(data.last_at));
          meta += chip('Eventos', data.event_count);
          document.getElementById('db-modal-meta').innerHTML = meta;
          document.getElementById('db-modal-events-list').innerHTML = (data.events || []).map(renderEvent).join('');
          document.getElementById('db-modal-loading').style.display = 'none';
          document.getElementById('db-modal-events').style.display = 'block';
        })
        .catch(function () { document.getElementById('db-modal-loading').textContent = 'Erro ao carregar. Tente novamente.'; });
    });
  });

  function closeModal() { document.getElementById('db-modal-overlay').style.display = 'none'; document.body.style.overflow = ''; }
  document.getElementById('db-modal-close').addEventListener('click', closeModal);
  document.getElementById('db-modal-overlay').addEventListener('click', function (e) { if (e.target === this) closeModal(); });
  document.addEventListener('keydown', function (e) { if (e.key === 'Escape') closeModal(); });
}());
</script>
<% end %>